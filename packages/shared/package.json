{
  "name": "shared",
  "version": "1.0.0",
  "private": true, // Verhindert versehentliches Veröffentlichen
  "main": "dist/index.js", // Kompiliertes JS
  "types": "dist/index.d.ts", // Typdefinitionen
  "scripts": {
    "build": "tsc" // TypeScript kompilieren
  },
  "devDependencies": {
    "typescript": "^5.x.x" // Beispielversion
  }
}